# 学习方式与推进规则

本文件定义本仓库后续学习的默认协作方式。目标是避免“看得懂 AI 写的代码，但自己从空文件写不出来”，并让 Python、C++、机器学习与工程实践互相促进。

## 一、默认学习闭环

每个重要知识点按以下五阶段推进。

### 1. Concept：先理解

先回答：

- 它是什么？
- 为什么需要它？
- 底层或运行机制是什么？
- 和相似方案有什么区别？
- 常见错误和边界条件是什么？
- 在机器学习、AI 工程或实际项目中哪里会出现？
- 面试中可能从什么角度追问？

文字讲解不必全部塞进 `.py/.cpp` 文件。代码文件只保留必要注释，较完整的概念说明可放在对应目录的 Markdown 笔记中。

### 2. Demo：看一份精简教学代码

Demo 的目的不是覆盖所有 API，而是用最少代码展示核心机制。

要求：

- 可以直接运行；
- 有明确输入与输出；
- 只保留关键注释；
- 必要时包含错误示例或对比实现；
- 优先使用接近真实工程的例子。

### 3. Practice：从空文件独立写

这是后续学习中最重要的一步。

默认情况下，练习阶段 AI 先只提供：

- 需求；
- 输入输出；
- 约束；
- 测试样例；
- 必要提示。

先独立实现，再进行代码审查。除非卡住，否则不直接复制 Demo。

### 4. Engineering：放进真实场景

把知识点至少连接到一个真实用途，例如：

- 装饰器 → retry / timing / cache；
- dataclass → 训练配置；
- logging → 模型训练日志；
- asyncio → 并发 API 调用；
- NumPy broadcasting → 批量特征计算；
- PyTorch Dataset → 数据流水线；
- C++ RAII → 资源管理；
- STL → 算法题和工程容器选择。

### 5. Check：确认真正掌握

一个知识点至少满足三项：

- **能解释**：不用照笔记，可以说清机制和适用场景；
- **能独立写**：从空文件完成核心实现；
- **能应用**：能在新问题或项目中正确使用。

只做到“看懂代码”不视为完成。

---

## 二、复习旧知识的方式

已经学过但遗忘的内容采用“诊断式复习”，而不是从头完整重学。

流程：

1. 先完成一个小任务或回答一个机制问题；
2. 能独立完成 → 跳过详细教学，只做关键点复盘；
3. 部分遗忘 → 针对薄弱处快速讲解并练习；
4. 基本忘记 → 重新走 Concept → Demo → Practice；
5. 最后把它连接到当前 ML 或工程代码。

当前 Python 优先复习：函数高级用法、OOP、装饰器、生成器、并发、异步、类型标注。

当前 C++ 优先复习：引用、指针、const、内存、类与对象生命周期，然后进入 RAII、STL、智能指针、移动语义、模板和工程工具。

---

## 三、机器学习课程与代码学习合并

机器学习不单独作为“只看公式”的课程，也不单独开一条“纯 Python API”路线。

遇到适合手写的算法时，优先采用：

```text
课程原理 / 数学
    ↓
NumPy 手写核心算法
    ↓
与 scikit-learn 结果对照
    ↓
PyTorch 实现（神经网络等适用内容）
    ↓
训练、评估、日志、配置、测试等工程化
```

重点不是重复造轮子，而是通过手写理解数据流、矩阵形状、损失函数、梯度和训练过程。

---

## 四、算法训练规则

算法不再按“收集题解”推进，而按模式学习：

- 数组 / 哈希
- 双指针
- 滑动窗口
- 二分
- 栈 / 队列
- 链表
- 树
- 堆
- 图
- DFS / BFS
- 回溯
- 贪心
- 动态规划

已有 `py_algo/` 继续保留。之后对典型题可采用：

1. Python 独立完成；
2. 复盘时间与空间复杂度；
3. 间隔一段时间后使用 C++ 重写；
4. 重点比较容器、类型、内存和 API 差异。

---

## 五、代码与笔记的边界

### 代码文件保留

- 核心实现；
- 必要注释；
- 输入输出示例；
- 容易踩坑的位置；
- 简短工程提示。

### Markdown 笔记保留

- 原理与机制；
- 对比与选型；
- 工程应用；
- 面试追问；
- 复习总结。

避免把一篇完整教程全部写进代码注释，也避免只保存 AI 生成的完整答案。

---

## 六、学习内容进入仓库的最低标准

新增内容优先满足以下至少一项：

- 补齐此前没有掌握的重要知识；
- 是一次有效的独立练习；
- 是机器学习算法的可解释实现；
- 展示工程化能力；
- 是可复用的小工具或小项目；
- 对实习面试复习有长期价值。

临时试验、一次性打印、重复度高的 AI 示例不必全部沉淀。
